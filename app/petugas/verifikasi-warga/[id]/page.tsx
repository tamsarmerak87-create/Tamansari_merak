import { PetugasWargaVerification } from "@/components/petugas/warga-verification";
export default async function Page({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <PetugasWargaVerification id={id} />; }